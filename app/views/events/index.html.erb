<h1><%= t('views.index_events') %></h1>

<div class="buffer-both">
  <%= link_to t('views.create_event_link'), new_event_path, class: 'btn btn-success' %>
</div>

<div class="row buffer-bottom">
  <%= render @events %>
</div>
